-- Évite d'avoir trop de fois les en-têtes qui s'affichent
SET PAGESIZE 0 EMBEDDED on;
-- On active l'affichage, qui préserve les espaces en début de ligne
SET SERVEROUTPUT ON FORMAT WRAPPED SIZE 10000;
